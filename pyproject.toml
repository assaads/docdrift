# pyproject.toml
[project]
name = "docdrift"
version = "0.1.0"
description = "Fail CI when your docs drift from your code — one manifest, any stack."
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = ["typer>=0.12", "pydantic>=2.7", "pyyaml>=6.0", "click>=8.1"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-mock", "ruff>=0.5", "mypy>=1.10"]

[project.scripts]
docdrift = "docdrift.cli:app"

[project.entry-points.pytest11]
docdrift = "docdrift.pytest_plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/docdrift"]

[tool.ruff]
line-length = 100
target-version = "py311"
[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
pythonpath = ["src"]
